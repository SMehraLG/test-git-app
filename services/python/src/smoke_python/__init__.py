"""Trivial arithmetic — the Python service in the factory monorepo smoke."""


def add(a: int, b: int) -> int:
    """Return the sum of ``a`` and ``b``."""
    return a + b


def subtract(a: int, b: int) -> int:
    """Return ``a`` minus ``b``."""
    return a - b
